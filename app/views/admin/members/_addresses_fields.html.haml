.nested-fields
  %table
    %tr
      %td
        = f.input :street, :wrapper => :inline, :label => false, :hint => 'name the task' do
          = f.input_field :street, :hint => 'type the name' 
      %td.done_column
        = f.input :done, :as => :boolean
      %td
        = link_to_remove_association "remove task", f

