%h1 
  = page_title 
= paginate @events
.miembros
  #nombres-miembros
    %ul
      - @events.each do |e|
        %li
          = link_to e.title, event_path(e)
%br
%br
= link_to 'Calendario', calendar_index_path
